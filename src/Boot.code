

def main(args: List[String]) {
    routes = new Routes()
    googleDAO = new DataProcDAO()
    dbDAO = new DatabaseDAO()
    clusterService = new ClusterService()

    monitor = new Monitor()
    monitor.resumeFromStartup()

    routes.listenForever()
}
